@"
package com.finvault;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class FinVaultApplication {
    public static void main(String[] args) {
        SpringApplication.run(FinVaultApplication.class, args);
    }
}
"@ | Out-File -FilePath "backend\src\main\java\com\finvault\FinVaultApplication.java" -Encoding UTF8server.port=8080
spring.datasource.url=jdbc:h2:mem:finvaultdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
finvault.jwt.secret=FinVaultSuperSecretKey2024ChangeInProductionMustBe256BitsLong
finvault.jwt.expiration=86400000
finvault.cors.allowed-origins=http://localhost:3000
logging.level.com.finvault=DEBUG
